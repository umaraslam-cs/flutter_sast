include: package:lints/recommended.yaml

analyzer:
  exclude:
    # Example has its own pubspec; analyze with `dart analyze example/`.
    - example/**
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    missing_required_param: error
    missing_return: error
    dead_code: warning
